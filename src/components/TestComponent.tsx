import React, { useState } from 'react';
import { SafeAreaView, View, Button, Alert, Text } from 'react-native'


const TestComponent = () => {
    <View>
        <Text> Here is a test component</Text>
    </View>
}

const style = {
    fontFamily: 'Roboto-Medium',
    fontSize: 28,
    color: '#333',
    marginBottom: 30,
}

export default TestComponent;